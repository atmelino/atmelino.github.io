
* {
  box-sizing: border-box;
}

/* Add a gray background color with some padding */
body {
  font-family: Verdana;
  padding: 20px;
  background: #f1f1f1;
}

/* Header/Blog Title */
.header {
  padding: 5px;
  font-size: 25px;
  text-align: center;
  background: white;
  border-radius: 5px;
}

/* Create two unequal columns that floats next to each other */
/* Left column */
.leftcolumn {
  float: left;
  width: 75%;
}

/* Right column */
.rightcolumn {
  float: left;
  width: 25%;
  padding-left: 20px;
}

  /*  image box*/
.imagebox {
    background-color: rgb(229, 228, 215);
    width: 100%;
    padding: 5px;
  }
    
/* Add a card effect for articles */
.card {
  background-color: white;
  padding: 20px;
  margin-top: 20px;
  border-radius: 5px;
}

.card_new {
  background-color: white;
  padding: 20px;
  margin-top: 20px;
  border-radius: 5px;
}

.innercard {
  background-color: rgb(219, 228, 228);
  padding: 10px;
  margin-top: 10px;
  border-radius: 5px;
}

.preserve_line{
  white-space: pre-line; 
}

/* Clear floats after the columns */
.row:after {
  content: "";
  display: table;
  clear: both;
}

/* Footer */
.footer {
  padding: 20px;
  text-align: center;
  background: #ddd;
  margin-top: 20px;
}

/* Responsive layout - when the screen is less than 800px wide, make the two columns stack on top of each other instead of next to each other */
@media screen and (max-width: 800px) {
  .leftcolumn,
  .rightcolumn {
    width: 100%;
    padding: 0;
  }
}

.code {
  background-color: rgb(224, 224, 228);
  border: 1px solid rgb(32, 27, 38);
  border-radius: 4px;
  padding: 4px 5px;
  margin-top: 5px;
  margin-bottom: 5px;
  font-family: Consolas, "Liberation Mono", Courier, monospace
}

.code_new {
  background-color: rgb(224, 224, 228);
  border: 1px solid rgb(32, 27, 38);
  border-radius: 4px;
  padding: 4px 5px;
  margin-top: 5px;
  margin-bottom: 5px;
  font-family: Consolas, "Liberation Mono", Courier, monospace;
  white-space: pre; 
}

.data {
  background-color: rgb(245, 241, 228);
  border: 1px solid rgb(32, 27, 38);
  border-radius: 4px;
  padding: 4px 5px;
  margin-top: 5px;
  margin-bottom: 5px;
  font-family: Consolas, "Liberation Mono", Courier, monospace
}

.file {
  background-color: rgb(246, 236, 203);
  border: 1px solid rgb(32, 27, 38);
  border-radius: 4px;
  padding: 4px 5px;
  margin-top: 5px;
  margin-bottom: 5px;
  font-family: Consolas, "Liberation Mono", Courier, monospace
}

.output {
  background-color: rgb(217, 223, 201);
  border: 1px solid rgb(32, 27, 38);
  border-radius: 4px;
  padding: 4px 5px;
  margin-top: 5px;
  margin-bottom: 5px;
  font-family: Consolas, "Liberation Mono", Courier, monospace
}

.flexbox {
  display: -ms-flex;
  display: -webkit-flex;
  display: flex;
  width: 100%;
}

.left {
  background: #a0ffa0;
  min-width: 75px;
  flex-grow: 0;
}

.right {
  background: #a0a0ff;
  flex-grow: 1;
}

#filenames {
  font-family: Arial, Helvetica, sans-serif;
  border-collapse: collapse;
  width: 100%;
}

#filenames td, #filenames th {
  border: 1px solid #ddd;
  padding: 8px;
}

#filenames tr:nth-child(even){background-color: #f2f2f2;}

#filenames tr:hover {background-color: #ddd;}

#filenames th {
  padding-top: 12px;
  padding-bottom: 12px;
  text-align: left;
  background-color: #04AA6D;
  color: white;
}

.question{
  background: #f1eae3;
  margin-top: 10px;
}

.answer{
  background: #daefe3;
  margin-bottom: 5px;
}

.question2{
  background: #f1eae3;
  /*margin-top: 10px;*/
}

.answer2{
  /*background: #daefe3;*/
  /*margin-bottom: 5px;*/
}

#headerline_table {
  font-family: Arial, Helvetica, sans-serif;
  width: 100%;
}

#headerline_cell {
  font-family: Arial, Helvetica, sans-serif;
  text-align: center;
}

#techtips_table {
  font-family: Arial, Helvetica, sans-serif;
  width: 100%;
  /*border: 1px solid black;*/
}

#techtips_cell {
  font-family: Arial, Helvetica, sans-serif;
  width: 33%;
}

#plain_table {
  font-family: Arial, Helvetica, sans-serif;
  width: 100%;
  border: 1px solid black;
}

#plain_cell {
  font-family: Arial, Helvetica, sans-serif;
  width: 33%;
  border: 1px solid black;
}
